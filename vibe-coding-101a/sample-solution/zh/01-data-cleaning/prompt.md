# 練習 01 提示語 - 資料清理

> 同一對話裡的連續訊息，一則一則送；後面接續前面，不重講前提。

**第 1 則** ｜ 設定情境並清出乾淨表格
> 我想清理一份問卷資料，需要的話你在背後處理，把結果給我看就好。請讀 `data/pre-test.xlsx` 和 `post-test.xlsx` 裡名為 `答題記錄(選項編號)` 的工作表。注意有兩個陷阱：第一列其實是大標題，真正欄位名稱在第二列；另外這張表的數字是「選項編號」，不是實際同意分數，選項 1 代表 7 分、選項 7 代表 1 分，所以請先轉成 `actual_score = 8 - option_number`。讀進來後只保留 `帳號` 欄，把 16 題依序改名成 Q1 到 Q16，存成 `_results/clean_pre.csv` 和 `clean_post.csv`。

**第 2 則** ｜ 接著做配對（不必重講前面）
> 現在用 `帳號` 找出前後測都做的學生，存成 `_results/matched_ids.csv`，並印給我看：前測幾人、後測幾人、配對幾人、只有前測的是誰、只有後測的是誰。

**第 3 則** ｜ 再做品質檢查
> 順便幫我檢查一下：所有作答是不是都在 1 到 7 之間？有沒有缺漏（空白）？

**第 4 則** ｜ 工具一致性（Bonus，練習 03 會回來）
> 還有一件事想確認：第 11 題在前測和後測的「題目文字」一樣嗎？如果被改寫過，那前後測還能不能直接比較？
