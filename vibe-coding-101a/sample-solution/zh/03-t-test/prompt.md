# 練習 03 提示語 - t 檢定

> 同一對話裡的連續訊息，一則一則送；後面接續前面，不重講前提。

**第 1 則** ｜ 設定情境並跑成對 t 檢定
> 我已經算好構面分數了，現在想請你幫我做 t 檢定，需要的話你在背後算就好。檔案都在 `_results/`：`subscale_scores_pre.csv`、`subscale_scores_post.csv`、`matched_ids.csv`。先只留前後測都做的 41 人、依帳號對齊，然後對 IM、IR、ER、AM 各做一次成對 t 檢定（方向＝後測減前測）。每個構面存到 `ttest_results.csv`：前測平均與標準差、後測平均與標準差、差值 ΔM、t 值、自由度、p 值，以及效果量 Cohen's dz（差值的平均除以差值的標準差）。

**第 2 則** ｜ 請代理逐一解讀（特別提醒 AM 方向）
> 把每個構面用一句白話解讀給我聽。特別提醒你：AM 是「無動機」，分數越高越糟，所以 AM 上升是壞消息，別讀反了。

**第 3 則** ｜ 請代理幫你想限制
> 哪些結論因為只有 41 人、或因為第 11 題在前後測被改寫，需要講得保守一點？

**第 4 則** ｜ 換個角度（Bonus，獨立 t 檢定）
> 換個方式：把 `data/grade-report.csv` 依 `final` 用全班中位數切成高分組和低分組，比較兩組的後測 IM。先用 `帳號`/`id` 把成績和問卷對起來。高分的學生，內在動機真的比較高嗎？這跟剛剛的成對檢定差在哪？
